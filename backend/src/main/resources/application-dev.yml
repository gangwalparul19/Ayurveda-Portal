spring:
  datasource:
    master:
      url: jdbc:mysql://localhost:3306/ayurveda_master?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
      username: root
      password: root@123

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.ayurveda.platform: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
